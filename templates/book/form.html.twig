{% extends 'base.html.twig' %}
{% block title %}Book form{% endblock %}
{% block body %}
    <h2>Book form</h2>
    {{ form_start(f) }}
        {{ form_widget(f) }}
        <button type="submit">Save</button>
    {{ form_end(f) }}
{% endblock %}